# GitHub Linguist: keep language stats focused on the Python pipeline.
# clusters.html is a generated face-cluster visualization dump (~5 MB).
facial/clusters.html  linguist-generated=true
# Frontend pages are hand-written but byte-heavy D3/HTML; down-weight so the
# repo reads as the Python project it is.
frontend/*.html       linguist-vendored=true
